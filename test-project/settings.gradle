rootProject.name = 'javafxplugintests'

if (JavaVersion.current().java9Compatible) {
    include 'modular'
}
include 'non-modular'
if (JavaVersion.current().java9Compatible) {
    include 'transitive'
}

